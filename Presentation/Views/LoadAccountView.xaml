<UserControl x:Class="Presentation.Views.LoadAccountView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Presentation.ViewModels">
    <UserControl.DataContext>
        <vm:LoadAccountViewModel />
    </UserControl.DataContext>

    <Grid Background="{StaticResource PageBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Accounts" 
                   Style="{StaticResource PageTitleStyle}"
                   Margin="40,20,40,10"/>

        <ListView Grid.Row="1" ItemsSource="{Binding Accounts}"
                  Margin="40,0,40,0" FontSize="13"
                  FontFamily="{StaticResource AppFont}"
                  Background="{StaticResource CardBackground}"
                  BorderBrush="{StaticResource CardBorder}"
                  BorderThickness="1">
            <ListView.View>
                <GridView ColumnHeaderContainerStyle="{StaticResource LeftAlignedHeader}">
                    <GridViewColumn Header="ID" Width="60" DisplayMemberBinding="{Binding Id}"/>
                    <GridViewColumn Header="Account Name" Width="200" DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="Group" Width="150" DisplayMemberBinding="{Binding Group}"/>
                    <GridViewColumn Header="Balance" Width="120" DisplayMemberBinding="{Binding Balance, StringFormat=N2}"/>
                </GridView>
            </ListView.View>
        </ListView>

        <Button Grid.Row="2" Content="Back" Command="{Binding BackCommand}"
                Style="{StaticResource SecondaryButtonStyle}"
                Width="140" HorizontalAlignment="Center" Margin="0,20,0,24"/>
    </Grid>
</UserControl>
