<UserControl x:Class="Presentation.Views.LoadCompanyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Presentation.ViewModels">
    <UserControl.DataContext>
        <vm:LoadCompanyViewModel />
    </UserControl.DataContext>

    <Grid Background="{StaticResource PageBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Load Company" 
                   Style="{StaticResource PageTitleStyle}"
                   Margin="40,20,40,10"/>

        <ListView Grid.Row="1" ItemsSource="{Binding Companies}"
                  SelectedItem="{Binding SelectedCompany}"
                  Margin="40,0,40,0" FontSize="13"
                  FontFamily="{StaticResource AppFont}"
                  Background="{StaticResource CardBackground}"
                  BorderBrush="{StaticResource CardBorder}"
                  BorderThickness="1">
            <ListView.View>
                <GridView ColumnHeaderContainerStyle="{StaticResource LeftAlignedHeader}">
                    <GridViewColumn Header="ID" Width="60" DisplayMemberBinding="{Binding Id}"/>
                    <GridViewColumn Header="Company Name" Width="250" DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="GSTIN" Width="150" DisplayMemberBinding="{Binding Gstin}"/>
                    <GridViewColumn Header="Country" Width="120" DisplayMemberBinding="{Binding Country}"/>
                    <GridViewColumn Header="State" Width="120" DisplayMemberBinding="{Binding State}"/>
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,24">
            <Button Content="Open" Command="{Binding OpenCommand}"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Width="140" Margin="0,0,12,0"/>
            <Button Content="Back" Command="{Binding BackCommand}"
                    Style="{StaticResource SecondaryButtonStyle}"
                    Width="140"/>
        </StackPanel>
    </Grid>
</UserControl>
